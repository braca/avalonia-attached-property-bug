<Styles xmlns="https://github.com/avaloniaui"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:controls="clr-namespace:AvaloniaAttachedProperty.Controls">

  <Style Selector="controls|GroupBox">
    <Setter Property="Background" Value="Red" />
    <Setter Property="controls:ColorZoneAssist.Mode" Value="{DynamicResource GenericAttributesGroupbox_ColorZoneAssist_Mode}" />

    <Setter Property="Template">
      <Setter.Value>
        <ControlTemplate>
          <Grid></Grid>
        </ControlTemplate>
      </Setter.Value>
    </Setter>
  </Style>
</Styles>